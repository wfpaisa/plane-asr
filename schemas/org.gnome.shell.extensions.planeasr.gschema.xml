<?xml version="1.0" encoding="utf-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.planeasr" path="/org/gnome/shell/extensions/planeasr/">
    <!-- ASR backend selection -->
    <key name="asr-backend" type="s">
      <default>'transcribe-cli'</default>
      <summary>ASR backend</summary>
      <description>Which transcription binary preset to use (whisper-cli, transcribe-cli, nero-asr, custom).</description>
    </key>
    <key name="cli-path" type="s">
      <default>''</default>
      <summary>Binary path</summary>
      <description>Absolute path to the transcription CLI binary.</description>
    </key>
    <key name="model-params" type="s">
      <default>''</default>
      <summary>Model parameters</summary>
      <description>Extra arguments passed to the binary (model path, language, ...). Quote paths with spaces.</description>
    </key>
    <key name="realtime-mode" type="b">
      <default>false</default>
      <summary>Realtime mode</summary>
      <description>Append --stream-chunk-ms 500 when supported by the backend.</description>
    </key>
    <key name="custom-arg-template" type="s">
      <default>'{cli} {params} {audio}'</default>
      <summary>Custom argument template</summary>
      <description>Template used when backend is 'custom'. Placeholders: {cli} {params} {audio}.</description>
    </key>
    <key name="chunk-enabled" type="b">
      <default>true</default>
      <summary>Live chunked transcription</summary>
      <description>Transcribe long recordings live in N-second chunks while recording, streaming each piece as it is ready, so backends with a per-call generation cap (e.g. Qwen3-ASR) don't truncate the output.</description>
    </key>
    <key name="chunk-seconds" type="i">
      <default>10</default>
      <summary>Seconds per chunk</summary>
      <description>Length in seconds of each chunk when 'chunk-enabled' is true. Lower values are safer for models that cap output tokens per call.</description>
    </key>
    <key name="chunk-overlap-seconds" type="i">
      <default>1</default>
      <summary>Overlap between chunks</summary>
      <description>Seconds of audio re-transcribed at the boundary between consecutive chunks so words split across the seam are not lost. The overlapping text is removed with fuzzy matching. 0 disables overlap.</description>
    </key>

    <!-- Output -->
    <key name="output-mode" type="s">
      <default>'clipboard'</default>
      <summary>Output mode</summary>
      <description>Where to send the transcribed text: 'clipboard' or 'paste'.</description>
    </key>

    <!-- Persisted results -->
    <key name="last-text" type="s">
      <default>''</default>
      <summary>Last transcription</summary>
      <description>The text produced by the last successful transcription.</description>
    </key>
    <key name="last-audio-path" type="s">
      <default>''</default>
      <summary>Last audio path</summary>
      <description>Absolute path of the last recorded WAV file.</description>
    </key>

    <!-- Global keybinding -->
    <key name="toggle-record-shortcut" type="as">
      <default><![CDATA[['<Super>A']]]></default>
      <summary>Toggle recording shortcut</summary>
      <description>Global keybinding that starts/stops recording.</description>
    </key>

    <!-- Debug -->
    <key name="debug-logging" type="b">
      <default>false</default>
      <summary>Debug logging</summary>
      <description>Log the ASR command line and its captured stdout/stderr to the system journal for troubleshooting.</description>
    </key>
  </schema>
</schemalist>
